# FYP Fix Plan — Audit → Implementation Roadmap

Generated from the code audit (see `docs/AUDIT_SUMMARY.md`). Status is updated as fixes land.

| Priority | # | Problem | Current Implementation | Required Fix | Files | Status |
|---|---|---|---|---|---|---|
| Critical | C1 | Price reconstruction uses the `open` column instead of `price` → every USD metric and every dashboard forecast wrong | `reconstruct_price(..., price_col_idx=0)`; column 0 is `open` | Reconstruct from the explicit previous **close**; remove the index-guessing API; unit test that true target reconstructs to the true close exactly | `src/utils/inverse_transform.py`, all callers | ✅ |
| Critical | C2 | Gold/Silver forward-filled to calendar days → 30% fabricated zero-return rows | `clean_data()` reindexes to `freq='D'` and `ffill`s for every asset | Keep the asset's own trading calendar; align macro data onto it | `src/data/preprocessing.py` | ✅ |
| Critical | C3 | Naive baseline beats every model; R² on price levels presented as skill; directional accuracy computed wrongly | `backtesting.py` metrics on USD only; `sign(prev-prev)=0` for naive; flat days counted | Return-space metrics, correct DA on non-flat days with binomial p-value, Diebold–Mariano test vs naive, strategy backtest; report honestly | `src/utils/metrics.py`, `src/evaluation/backtesting.py` | ✅ |
| Critical | C4 | Hyperparameters chosen on the test set (April LSTM experiments), never re-tuned | `BEST_*_CONFIG` hard-coded from test-set experiments | Expanding-window walk-forward grid search on train+val only; test set never loaded by the tuner | `src/training/tune_models.py`, `config.py` | ✅ |
| Critical | C5 | Served "primary" stacked ensemble is a constant predictor, never evaluated on test, and `prediction.py`/API crash (LightGBM given 900 features) | `reshape(1,-1)` flattening; `last_close` read from scaled CSV; stale `*_meta_model.pkl` | Rewrite inference around the evaluated final model per asset; ensemble kept as a reported experiment | `src/inference/prediction.py`, `src/api/app.py`, `app/streamlit_app.py`, `config.py` | ✅ |
| High | H1 | CatBoost early-stops on data inside its own training set | `train_catboost(X_tr, ...)` where `X_tr ⊃ X_val` | Phase-1 fit on train with val eval → best iteration; phase-2 refit on train+val with fixed iterations | `src/models/catboost_model.py`, training | ✅ |
| High | H2 | Non-stationary price-level features (open/high/low/price/EMA/BB/VWAP) scaled on train → out of range on test | Raw levels in the feature matrix | Replace with stationary ratios (price/EMA−1, %B, bandwidth, ATR/price, MACD/price, HL range); keep raw columns only for charts | `src/data/preprocessing.py` | ✅ |
| High | H3 | Stale result CSVs/plots from the price-target era mixed with new results | `src/results/*` | Archive to `results/archive_2026-04/`; regenerate everything into `results/` | `results/`, `config.py` | ✅ |
| High | H4 | Stacking OOF trains GRU for 5 epochs but deploys a fully trained GRU; config base-model order ≠ training order; two meta-model filenames | `ensemble_model.py`, `config.MODEL_STATUS` | OOF uses the same tuned config as deployment; single artefact naming; order stored with the model | `src/models/ensemble_model.py` | ✅ |
| High | H5 | No train-set metrics → overfitting cannot be shown; no saved loss curves for GRU | Only test metrics | Save train/val/test metrics and Keras histories for every model | `src/training/train_models.py` | ✅ |
| Medium | M1 | README/docstrings claim PatchTST, TFT, Williams %R, CCI, Stochastic, "30+ indicators" — none exist | README, train script docstrings | Accurate documentation | `README.md`, `docs/` | ✅ |
| Medium | M2 | Makefile / retrain.py / CI reference non-existent scripts; 4/31 tests fail; `config.RESULTS_DIR` wrong | `train_lgbm.py`, `DataCollector`, `SMA_7` tests | Point to real scripts; fix tests | `Makefile`, `scripts/retrain.py`, `.github/workflows/ci.yml`, `tests/` | ✅ |
| Medium | M3 | Redundant features (BB_Mid≈EMA, VWAP≈price, ROC≈return_10d) and redundant model (CatBoost≈LightGBM) | — | Remove redundant features; keep CatBoost only as a comparison row | preprocessing, docs | ✅ |
| Medium | M4 | Dashboard lists stale LSTM (16-feature) models and an `Ensemble` option with no code path | `get_available_models`, `run_model_prediction` | Rebuild inventory from `MODEL_STATUS`; every listed model has a working branch | `app/streamlit_app.py` | ✅ |
| Low | L1 | Notebooks have no outputs and reference `n_features=16`, `../results/` | `notebooks/*.ipynb` | Update references; notebooks are companions, scripts are the source of truth | `notebooks/` | ✅ |
| Low | L2 | Only 3 years of history (`period="3y"`) | `data_collection.py` | Support `start=` date; documented as a limitation (Yahoo rate-limited during this work, so existing raw data is used) | `src/data/data_collection.py` | ✅ |
| **Critical (final audit)** | C6 | Close-time misalignment for the metals: Yahoo's GC=F/SI=F daily close is the 13:30 ET settlement, but same-day S&P/VIX/TNX/DXY/WTI returns (fixed 14:30–17:00 ET) and the session High/Low (`hl_range`, `atr_norm`, `ADX`) were used as day-*t* features → information from inside the target interval; produced a spurious 62 % Silver directional accuracy | `add_external_features` reindexed same-day values for every asset | Macro returns aligned to the latest external date **strictly before** *t* for commodities (same-day kept for BTC, whose bar closes after the US close); High/Low-based features lagged one session for the metals; both rules unit-tested; full pipeline re-run | `config.py` (`EXTERNAL_SAME_DAY`, `POST_SETTLEMENT_FEATURES`), `src/data/preprocessing.py`, `tests/test_features.py` | ✅ |
| Medium (final audit) | M5 | Live sync overwrote the frozen `data/raw/` macro files and never refreshed gold for Silver's `gold_return` | `fetch_all_external_data()` wrote to `RAW_DATA_DIR` | All live downloads go to `data/raw_live/` (git-ignored); `DataCleaner(asset, raw_dir=…)`; gold refreshed for Silver | `src/data/sync_live_data.py`, `data_collection.py`, `external_data.py` | ✅ |
| Medium (final audit) | M6 | `make preprocess` / `make collect-data` failed (`No module named config`) unless run through `scripts/retrain.py` | missing `sys.path` bootstrap | Bootstrap added, consistent with every other script | `src/data/preprocessing.py`, `src/data/data_collection.py` | ✅ |
| Low (final audit) | L3 | Demo look-ahead test only called the function twice; Keras phase A used an LR schedule phase B could not; BTC Sharpe annualised with 252 days | — | Test now perturbs every future row; phase A/B identical procedures (early stopping only); 365 periods for crypto | `tests/`, `src/models/registry.py`, `src/utils/metrics.py` | ✅ |
